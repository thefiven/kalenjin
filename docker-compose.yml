services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kalenjin
      POSTGRES_PASSWORD: kalenjin
      POSTGRES_DB: kalenjin
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
